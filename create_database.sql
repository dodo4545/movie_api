-- Create the movie_api database
-- Run this first, then reconnect to the new database

CREATE DATABASE movie_api;

-- After creating the database, you'll need to:
-- 1. Create a new connection in SQLTools with database = 'movie_api'
-- 2. Run your database_setup.sql script in the new database